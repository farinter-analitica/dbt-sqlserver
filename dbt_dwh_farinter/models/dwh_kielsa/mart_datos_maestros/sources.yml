sources:
  - name: DL_FARINTER
    description: "Base de tablas cargadas (Data Lake)"
    database: DL_FARINTER
    schema: dbo
    freshness: # default freshness
        warn_after: {count: 28, period: hour}
        error_after: {count: 72, period: hour}
    loaded_at_field: Fecha_Actualizado
    #group: ldcom_etl_dwh #se definió en dbt_project.yml
    #meta:
    #    dagster:
    #        group: "ldcom_etl_dwh"

    tables:
      - name: placeholder_mart_datos_maestros # es mejor usar una estructura parecida a las bases de datos (carpeta dl_farinter)